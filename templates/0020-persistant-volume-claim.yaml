apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pods-content
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: do-block-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
